class Store {
  final String name;
  final String address;
  final String logo;
  final String banner;
  final bool isVerified;

  Store(this.name, this.address, this.logo, this.banner, this.isVerified);
}
