import 'package:flutter/material.dart';

class BottomNavIndex extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        child: Center(
          child: Text("Bottom nav page"),
        ),
      ),
    );
  }
}
